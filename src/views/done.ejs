<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Done</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="<%= bodyClass %>">
    <header class="bar">
        <div class="brand">Multi-Form Session</div>
        <nav class="nav">
            <a href="/">Home</a>
            <a href="/cookies">Cookies</a>
            <a href="/profile">Profile</a>
            <a href="/signup/step/1">Multi-Form</a>
            <a href="/status">Status</a>
        </nav>
    </header>

    <main class="container">
        <h1>Done</h1>
        <p>This is what would be “saved to the database”:</p>

        <pre class="code"><%= JSON.stringify(signup, null, 4) %></pre>

        <form method="POST" action="/signup/reset" class="row">
            <button type="submit">Start Over</button>
        </form>
    </main>
</body>
</html>